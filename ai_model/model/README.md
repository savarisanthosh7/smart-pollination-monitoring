# Trained Model

The trained model file is intentionally not committed because the original trained weights were not available for recovery.

After training, the expected artifact is:

```text
beewatch_cnn.keras
classes.txt
```

Store large model artifacts using Git LFS or an approved model registry if they are to be versioned.
