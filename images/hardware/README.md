# 🔩 Hardware Evidence

Use this folder for original prototype photographs and local display evidence.

- `prototype.jpg` — assembled hardware prototype
- `lcd-bee.jpg` — LCD showing bee classification
- `lcd-noise.jpg` — LCD showing noise classification

The main README intentionally uses only the clearest prototype image so the page stays clean.